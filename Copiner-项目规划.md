# Copiner 项目规划

> 一款面向企业内部、协助处理**非代码类工作**的 AI Agent 桌面工具（对标 Codex / Claude Code 的交互形态，但业务面向办公与运维场景）。
>
> **v2 修订说明**：本版依据《一些关于 Harness 思考分享》的 Code & Cowork 部分重构。核心转变是——
> Copiner 的产品本体**不是"又一个带工具调用的聊天框"，而是一套 Harness（Agent 配置体系）**。
> 模型是别人的、会一直变；沉淀下来属于我们的是：全局/场景 Agent、渐进式披露的能力手册、
> 提问→Plan→执行的闭环、以及"费曼总结→归档→RAG 切分"的知识飞轮。

---

## 一、项目定位

| 维度 | 说明 |
|------|------|
| **产品名** | Copiner（Co-worker + Partner 之意）|
| **一句话** | 员工只和**一个 Agent** 对话，由它去和邮件、问题单、eHR、BIOS/BMC 资料、代码仓库打交道 |
| **核心价值** | 把"AI 问答 + 自动化执行 + 企业系统集成"打包成可在公司内网/本地运行的桌面 Agent |
| **真正的护城河** | **Harness 层**：个体化适配的 Agent 配置 + 闭环工作流 + 越用越聪明的记忆体/知识库 |
| **对标产品** | Codex / Claude Code（交互与 Agent 循环）、CC-Switch（多 API 路由）、各类 RPA 工具 |
| **目标用户** | 公司内部员工（运维、行政、支持、测试等非纯研发岗）|
| **差异点** | ① 原生多路由 API；② 深度 OEM 场景（BMC/BIOS、邮件、内部系统工作流）；③ 云上云下双端互通；④ **Harness 可移植、可自更新** |

### 设计隐喻：圆形方孔铜钱

沿用分享中的框图，Copiner 的架构直接落在这个隐喻上：

- **方（本地端 / Code 侧）**：桌面客户端 + Agent 内核 + 本地工具，数据不出内网，敏感操作在本地完成。
- **圆（云上 Workspace / Cowork 侧）**：企业系统中转、RAG 知识库、记忆体、任务队列、跨端同步。
- **孔（连接）**：以公司系统网站为可信中转的双端通道——本地能力与云上上下文互通。

> 寓意：希望大家在 AI 的助力下，能更轻松地赚到更多的钱。

---

## 二、Harness 层（新增，最高优先级）

> 这是 v1 规划完全缺失的一层，也是分享中被反复验证有效的部分。
> **它必须在 M1 就存在**，而不是等功能做完再补配置——否则后面每个场景都会退化成散装 prompt。

### 2.1 全局 Agent（`COWORK.md`）—— 个体化适配

| 机制 | 做法 | 为什么 |
|------|------|--------|
| **自我介绍 Onboarding** | 首次启动引导用户做一次自我介绍（岗位、技术背景、熟悉的知识锚点、沟通偏好），由 AI 生成该用户的全局 Agent | 分享中的关键发现：正因为做了自我介绍，AI 才知道该用「处理器流水线 / 编译器」这类**用户已有的知识**去解释抽象概念 |
| **苏格拉底式对话** | 默认不做"你说得对"式附和；对模糊立场先拆解概念，再以提问接生结论 | 要的是**思辨的产物**，不是廉价的肯定 |
| **费曼学习法** | 任务完成后 Agent 要"讲给用户听"，用户可反问；讲不明白 = 没做对 | 听懂和会用都不等于学会，讲明白才是 |
| **提问优先** | 需求模糊时**先提问**再动手（"便携指什么？预算多少？"） | 不要让用户在"百事还是可口"上二次思考——但也不要在信息不足时瞎猜 |

### 2.2 场景级 Agent + 渐进式披露

**原理**：`AGENTS.md` 每次会话开始都会被完整读入上下文 → 会话越长，Token 成本越高，且 Agent 会**逐渐忽略**其中的配置。

**解法**：主文件保持 **≤ 30 行**，只放"我是谁 / 有哪些手册 / 什么时候去读哪本"；细则拆到独立文件，**用到时才加载**。

```
copiner/
├── COWORK.md                    # 全局：个体化人设 + 工作流铁律（≤30行）
├── skills/
│   ├── mail/SKILL.md            # 邮件场景手册
│   ├── ticket/SKILL.md          # 问题单/系统流程手册
│   ├── bios-bmc/SKILL.md        # BIOS/BMC 资料检索 + 日志归因手册
│   └── code-sync/SKILL.md       # 多分支同码手册
├── references/                  # 按需加载的重资料（API 契约、字段字典、报文样例）
└── hooks/session-start.*        # 会话启动时动态注入上下文
```

**收益**：Token 大幅下降 + 显著降低幻觉（该记的时候才让它记）。

### 2.3 Hook —— 动态基线注入

静态文档解决不了"每次会话基线不同"的问题。用 **SessionStart Hook** 在会话开始时动态执行指令、载入上下文：

- 今日未读/待办邮件摘要
- 当前值班机型与基线版本（Turin / Genoa / …）
- 当前 Git 分支与待同步分支列表
- 用户名下未闭环的问题单

### 2.4 闭环工作流（产品级铁律）

```
提问澄清 → 列 Todos → 生成 Plan（人工确认）→ 执行 → 费曼总结 → 归档入 RAG
```

这不是"建议流程"，而是**内核强制**的状态机。每个场景 Agent 都跑同一套闭环，UI 上对应四个可见阶段（澄清卡 / Todo 面板 / Plan 文档 / 执行日志）。

### 2.5 禁令与红线（Constraints）

在每份场景手册末尾配置禁令。**所有对外产生副作用的动作，一律 Plan 后人工确认，Agent 不得自动执行**：

- ❌ 未经确认发送邮件 / 提交电子流 / 下发问题单
- ❌ 未经确认 push 代码或合并分支（只允许建分支 + 同步 + 待人工判断）
- ❌ 越权读取他人邮箱、跨部门系统数据
- ❌ 把敏感日志（BMC/BIOS/客户信息）送到外部模型 —— 走本地/内网模型路由

### 2.6 记忆体（Memory）

- 个体化记忆：用户偏好、常用机型、历史归因结论、踩过的坑。
- **可移植**：记忆体与 Skill 均为纯文本，可跨平台基线迁移、可手动/自主更新。
- 长期价值：当能力与知识被 AI 弱化，**经验与智慧**才是个体的真正价值——记忆体就是它的载体。

---

## 三、功能模块拆解

### 第 0 层：Harness（见第二章）
全局 Agent / 场景 Skill / 渐进式披露 / Hook / 闭环工作流 / 禁令 / 记忆体。

### 第 1 层：AI 内核（Core Engine）
- **AI 问答能力（API 驱动）**
  - 抽象统一的 LLM Provider 接口（OpenAI / Anthropic / 国产模型 / 自建）
  - **原生 CC-Switch 式路由**：按模型、成本、可用性、任务类型自动/手动切换
  - **敏感数据强制路由**：命中敏感标签的上下文只允许走内网/本地模型
  - Key 池管理、限流、失败重试与降级
  - Agent 循环：工具调用（Tool Use）、多轮上下文、记忆管理
  - **Plan Mode**：内核原生支持"只规划不执行"的模式切换

### 第 2 层：能力扩展（Extensibility）
- **自动化引擎**
  - 任务编排（触发器 → 步骤 → 条件分支 → 输出）
  - 定时任务 / 事件触发 / 手动触发
  - 可录制"操作流"并回放
- **插件 / Skill 系统**
  - 标准协议（清单 + 权限声明 + 生命周期钩子）
  - **Skill 即插件**：一个场景包 = 手册 + 工具 + 禁令，可独立分发/更新
  - 沙箱隔离，权限最小化

### 第 3 层：交互与体验（UX）
- **导入 Codex 桌宠**
  - 桌宠资源导入 / 适配层（形象、动画、状态绑定 AI 运行态）
  - 作为常驻入口与状态可视化载体：**澄清中 / 规划中 / 执行中 / 待确认** 四态一眼可见

### 第 4 层：Cowork 场景套件（OEM Suite）

> 以下四项直接对应分享中 Cowork 的四类便利性应用，已从 v1 的抽象描述细化为可验收的场景。

#### C1. 邮件管理
- 每天只推**摘要 + 个体化侧重**（不是 Outlook 那种死规则分类）
- **请假邮件 → 自动截图关联 eHR，提交电子流**（Plan 后人工确认）
- **cdms 邮件 → 自动登录系统下行问题单**
- 收发、分类、自动回复草稿、附件处理

#### C2. 公司系统管理（统一 Agent 门面）
- 痛点：公司各系统都不好用，问题单系统还是华三 5 年前的剩饭
- 目标：**员工只和一个 Agent 对接**，由它去和 eHR / cdms / 问题单 / 审批系统的 API 交互
- 实现：优先官方 API；无 API 的走浏览器自动化兜底

#### C3. 文档速查（RAG）
- 痛点：云尖智擎那边的硬件文档检索，**连 Intel 的常用文档都搜不到**
- 目标：做一套**自己用的 BIOS / BMC 资料检索工具**
- 复用：BMC/BIOS **日志一键解析**（抓取 → 结构化 → AI 归因 → 报告）与本 RAG 共用同一套知识库

#### C4. 编码助手（Cowork 侧的轻量协助，非替代 Code 侧）
- **多分支同码**：Turin & Genoa 等多套分支上传同样代码的冗余工作 → 触发后在目标仓库**自动建 Branch + 同步代码**，由人判断后再提交
- **总结归档**：配合费曼学习法，任务完成后自动产出总结文档 → **自动 RAG 切分入库**

#### C5. 云上云下双端互通
- 以公司系统网站作可信中转，实现本地端（方）与云上 Workspace（圆）的状态/任务同步

### 后续规划
- **微信小程序远程访问** —— 手机端远程下发指令、查看结果、**审批 Plan**（红线动作的移动端确认位）

---

## 四、知识飞轮（Copiner 的复利机制）

这是让 Copiner "越用越聪明"的闭环，也是 v1 完全没有的部分：

```
   执行任务
      │
      ▼
  费曼式总结  ──── Agent 讲给用户听，用户反问、纠偏
      │
      ▼
   文档归档   ──── 结论、归因、踩坑写成结构化文档
      │
      ▼
  自动 RAG 切分入库
      │
      ▼
  下次检索命中 ──→ 更准的归因 / 更少的提问 ──→ 回到执行任务
```

**衡量指标**：不看"AI 使用率排名"（那和剩余工时一样荒谬），看
① 闭环完成的任务数；② 单任务节省工时；③ RAG 命中率与复用次数。

---

## 五、技术架构

```
┌─────────────────────────────────────────────────────────┐
│   方 · 桌面客户端 (Electron/Tauri)                        │
│   桌宠层 │ 对话UI │ Plan/Todo 面板 │ 插件宿主              │
├─────────────────────────────────────────────────────────┤
│   Harness 层（配置即产品）                                │
│   COWORK.md │ Skills │ 渐进式披露 │ Hooks │ 禁令 │ 记忆体   │
├─────────────────────────────────────────────────────────┤
│   Agent 内核 (本地服务)                                   │
│   路由器 │ Agent循环 │ Plan Mode │ 工具注册表 │ 上下文管理   │
├──────────────┬────────────┬──────────┬──────────────────┤
│ LLM Providers│ 自动化引擎  │ 插件沙箱  │ OEM 适配器        │
├─────────────────────────────────────────────────────────┤
│   孔 · 企业侧中转：网站反向通道 / 鉴权 / 同步              │
├─────────────────────────────────────────────────────────┤
│   圆 · 云上 Workspace                                    │
│   RAG 知识库 │ 记忆体 │ 任务队列 │ 系统 API 网关            │
└─────────────────────────────────────────────────────────┘
```

**关键技术选型（待评审）**

| 组件 | 候选方案 |
|------|----------|
| 桌面框架 | Tauri（轻量、Rust 后端）或 Electron（生态成熟） |
| 后端/内核 | Rust / Go / Node.js（取决于团队栈） |
| 浏览器自动化 | Playwright / Puppeteer |
| 插件运行时 | WASM 沙箱 / Node VM / 独立进程 |
| **RAG** | **本地向量库（内网数据不出网）+ 结构化切分；BIOS/BMC 文档需按章节+表格感知切分** |
| **记忆体** | **纯文本 + 索引文件（可 diff、可迁移、可人工编辑）** |
| 双端互通 | WebSocket + 企业网站作反向通道，或消息队列 |
| 桌宠 | Live2D / Spine / 序列帧适配 |

---

## 六、开发路线图（里程碑）

> **调整要点**：Harness 提到 M1；BIOS/BMC 文档速查（C3）提前——它是痛点最真实、最快能出 Demo 的场景。

### M0 — 立项与设计（2~3 周）
- 需求细化、技术选型评审、架构定稿、安全合规评估
- **产出首版 `COWORK.md` 与一份场景 Skill 样板（哪怕内核还没写）**

### M1 — MVP：内核 + Harness 同时可用（4~6 周）
- 统一 Provider 接口 + CC-Switch 式路由（含敏感数据强制内网路由）
- 基础对话 UI + Agent 循环 + **Plan Mode**
- **全局 Agent + 自我介绍 Onboarding**
- **渐进式披露的 Skill 加载机制 + SessionStart Hook**
- **闭环工作流状态机（提问 → Todos → Plan → 执行）**
- 配置管理（Key 池、路由规则）

### M2 — 知识飞轮 + 场景首发（4~6 周）
- **C3 文档速查（RAG）：BIOS/BMC 资料检索** ← 优先出 Demo
- BMC/BIOS 日志一键解析（复用现有安全分析能力，与 RAG 共库）
- **费曼总结 → 自动归档 → RAG 切分** 闭环打通
- 记忆体 v1

### M3 — 扩展能力与体验（4~6 周）
- 自动化引擎（触发器 + 步骤编排）
- 插件/Skill 系统（协议 + 沙箱 + 1~2 个示例）
- 桌宠导入与四态绑定

### M4 — Cowork 套件 A（6~8 周）
- **C1 邮件管理**（摘要 + 个体化侧重；请假→eHR 电子流；cdms→问题单）
- **C4 多分支同码**（Turin/Genoa 自动建 Branch + 同步，人工提交）

### M5 — Cowork 套件 B（6~8 周）
- **C2 公司系统统一 Agent 门面**
- **C5 云上云下双端互通**

### M6 — 后续（规划中）
- 微信小程序远程访问（含 Plan 移动端审批）
- 插件市场、多用户/团队协作、**记忆体共享与碰撞**

---

## 七、风险与关注点

| 类别 | 风险 | 应对 |
|------|------|------|
| **安全合规** | 企业数据、BMC/BIOS/邮件涉敏感信息 | 本地优先、脱敏、权限分级、审计日志、**敏感标签强制内网路由** |
| **红线动作** | Agent 自动发件/提单/提交代码造成事故 | **Plan 后人工确认为硬约束**，写进每份 Skill 的禁令区 |
| **上下文腐化** | 会话变长后 Agent 忽略配置、Token 飙升 | **渐进式披露** + 主文件 ≤30 行 + 长会话主动提示新建会话 |
| **API 稳定性** | 多路由切换的一致性与幂等 | 路由降级策略、结果缓存 |
| **网站代理** | 内部系统改版导致自动化失效 | 优先用官方 API，UI 自动化做兜底 + 自愈 |
| **双端互通** | 穿透/鉴权/数据同步冲突 | 以企业网站为可信中转，做冲突合并策略 |
| **插件安全** | 第三方插件越权 | 沙箱 + 权限声明 + 签名审核 |
| **推广落地** | 各人 AI 使用程度差异大，易变成"刷使用率" | **不做使用率排名**；以闭环任务数/节省工时衡量；允许慢下来 |

---

## 八、下一步建议

1. **先写 `COWORK.md`，再写第一行代码**：Harness 是本项目的骨架，不是装饰。M0 就要有可评审的全局 Agent + 一份 Skill 样板。
2. **确认技术栈**：桌面框架 + 内核语言，决定后续所有实现。
3. **明确"公司系统网站"细节**：是否有开放 API？鉴权方式？这直接影响 C2、C5 的难度。
4. **优先 Demo：BIOS/BMC 文档速查（C3）**——痛点最真实（Intel 文档都搜不到）、已有技能积累、能最快跑通"费曼总结→归档→RAG"的完整飞轮。

---

> v1 为初版功能规划；v2 依《一些关于 Harness 思考分享 · Cowork》重构，补齐 Harness 层与知识飞轮。
> 这份文档同样不是一个结果，只是一个过程——它需要更多想法的碰撞来完善。
